# Example: basic passing doc ✨

> 🌼 A tiny happy-path example you can run in seconds.

```bash id=example-check expectExit=0 expectStdout=status expectJson=example-json
printf '{"status":"ok","service":"docs-drift"}'
```

```json id=example-json
{"service":"docs-drift","status":"ok"}
```
